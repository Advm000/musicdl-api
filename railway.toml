[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn main:app --bind 0.0.0.0:$PORT --worker-class gthread --workers 1 --threads 8 --timeout 300"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
